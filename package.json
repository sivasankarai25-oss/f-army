{
  "name": "farmy",
  "version": "1.0.0",
  "private": true,
  "description": "FARMY — Smart Farming Assistant. Static frontend + Vercel serverless API.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node -e 'console.log(\"Build success\")'",
    "start": "node server.js",
    "dev": "node server.js",
    "seed": "node scripts/seedCrops.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.0"
  }
}
